// TODO add contracts as comment to help
const /*CONTRACT_NAME*/ = require('../../contracts//*CONTRACT_NAME*/.json');

module.exports.signature = '/*EVENT_SIGNATURE*/';
module.exports.hexSignature = '/*EVENT_HEX_SIGNATURE*/';

module.exports.process = async (log) => {
  //const event = utils.getEvent(/*CONTRACT_NAME*/, '/*EVENT_SIGNATURE*/', log);

  return await commit(log);
};

async function commit(log) {
  return [log];
}
