const /*CONTRACT_NAME*/ = require('../../contracts//*CONTRACT_NAME*/.json');
const utils = require('../utils.js');

module.exports = async (log) => {
  const event = utils.getEvent(/*CONTRACT_NAME*/, '/*EVENT_NAME*/', log);

  return await commit(event);
};

async function commit(log) {
  return [log];
};
